# Pinned for reproducible deploys (Streamlit Community Cloud). Bump deliberately.
streamlit==1.62.0      # .st-key-<key> wrappers, st.popover(icon=), :material/ icons, st.container(key=)
requests==2.32.3       # app.py -> POST /query on the RAG backend

# Not needed yet — uncomment once retrieval is wired up to the real
# ChromaDB/LangChain backend (kept out for now so the mockup stays light):
# langchain
# chromadb
